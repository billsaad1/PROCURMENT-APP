<UserControl x:Class="JaahdLogistics.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JaahdLogistics.Views"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBlock Text="{DynamicResource Projects}" DockPanel.Dock="Top" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <UniformGrid DockPanel.Dock="Bottom" Columns="2" Margin="0,10,0,0">
                <Button Content="{DynamicResource AddProject}" Command="{Binding AddProjectCommand}" Margin="0,0,5,0"/>
                <Button Content="{DynamicResource Delete}" Command="{Binding DeleteProjectCommand}" Background="#C0392B"/>
            </UniformGrid>
            <ListBox ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Code}" FontWeight="Bold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <DockPanel Grid.Column="1" Visibility="{Binding SelectedProject, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{DynamicResource BudgetLines}" DockPanel.Dock="Top" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="{DynamicResource Name}" Margin="5"/>
                    <TextBox Grid.Column="1" Text="{Binding SelectedProject.Name, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    
                    <TextBlock Grid.Row="1" Text="{DynamicResource Code}" Margin="5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedProject.Code, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    
                    <TextBlock Grid.Row="2" Text="{DynamicResource Year}" Margin="5"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedProject.Year, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <TextBlock Grid.Row="3" Text="{DynamicResource ProjectManager}" Margin="5"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedProject.ProjectManager, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                    <TextBlock Grid.Row="4" Text="Project Officer" Margin="5"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SelectedProject.ProjectOfficer, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                </Grid>
            </StackPanel>

            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="{DynamicResource AddBudgetLine}" Command="{Binding AddBudgetLineCommand}" Width="150" Margin="0,0,10,0"/>
                <Button Content="{DynamicResource Save}" Command="{Binding SaveCommand}" Width="100"/>
            </StackPanel>
            
            <DataGrid ItemsSource="{Binding BudgetLines}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Code}" Binding="{Binding Code}" Width="Auto"/>
                    <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="Auto"/>
                    <DataGridTextColumn Header="{DynamicResource Description}" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource Unit}" Binding="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTextColumn Header="{DynamicResource Quantity}" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTextColumn Header="{DynamicResource UnitPrice}" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                    <DataGridTextColumn Header="{DynamicResource Amount}" Binding="{Binding TotalAmount, Mode=OneWay}" IsReadOnly="True" Width="Auto"/>
                    <DataGridTextColumn Header="{DynamicResource TotalSpending}" Binding="{Binding Spent, Mode=OneWay, StringFormat=N2}" IsReadOnly="True" Width="Auto"/>
                    <DataGridTextColumn Header="{DynamicResource Budget}" Binding="{Binding Remaining, Mode=OneWay, StringFormat=N2}" IsReadOnly="True" Width="Auto"/>
                    <DataGridComboBoxColumn Header="{DynamicResource Currency}" SelectedItemBinding="{Binding Currency}" Width="Auto">
                        <DataGridComboBoxColumn.ElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemsSource">
                                    <Setter.Value>
                                        <CompositeCollection>
                                            <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">USD</sys:String>
                                            <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">YER</sys:String>
                                        </CompositeCollection>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.ElementStyle>
                        <DataGridComboBoxColumn.EditingElementStyle>
                            <Style TargetType="ComboBox">
                                <Setter Property="ItemsSource">
                                    <Setter.Value>
                                        <CompositeCollection>
                                            <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">USD</sys:String>
                                            <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">YER</sys:String>
                                        </CompositeCollection>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridComboBoxColumn.EditingElementStyle>
                    </DataGridComboBoxColumn>
                    <DataGridTemplateColumn Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="{DynamicResource Delete}" Command="{Binding DataContext.DeleteBudgetLineCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" Background="#C0392B" Padding="5,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
