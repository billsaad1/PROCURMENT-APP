<UserControl x:Class="JaahdLogistics.Views.PurchaseRequisitionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:JaahdLogistics.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource PR}" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource PRNumber}" VerticalAlignment="Center" Margin="5"/>
            <TextBox Grid.Column="1" Text="{Binding CurrentPR.PRNumber}" Margin="5"/>

            <TextBlock Grid.Column="2" Text="{DynamicResource Project}" VerticalAlignment="Center" Margin="5"/>
            <ComboBox Grid.Column="3" ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" DisplayMemberPath="Name" Margin="5"/>

            <TextBlock Grid.Row="1" Text="{DynamicResource Currency}" VerticalAlignment="Center" Margin="5"/>
            <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Currencies}" SelectedItem="{Binding CurrentPR.Currency}" Margin="5"/>

            <TextBlock Grid.Row="1" Grid.Column="2" Text="{DynamicResource ExchangeRate}" VerticalAlignment="Center" Margin="5"/>
            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding CurrentPR.ExchangeRate}" Margin="5"/>
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding CurrentPR.Items}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="{DynamicResource BudgetLine}"
                                        ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.BudgetLines}"
                                        SelectedValueBinding="{Binding BudgetLineId}"
                                        SelectedValuePath="Id"
                                        DisplayMemberPath="Code"
                                        Width="100"/>
                <DataGridTextColumn Header="{DynamicResource Description}" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource Unit}" Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="{DynamicResource Quantity}" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="{DynamicResource UnitPrice}" Binding="{Binding UnitPrice}" Width="80"/>
                <DataGridTextColumn Header="{DynamicResource Total}" Binding="{Binding TotalPrice, Mode=OneWay}" IsReadOnly="True" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10">
            <Button Content="{DynamicResource AddItem}" Command="{Binding AddItemCommand}" Padding="15,5" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding BudgetWarning}" Foreground="Red" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Save}" Command="{Binding SavePRCommand}" Width="120" Height="35" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
        </StackPanel>
    </Grid>
</UserControl>
