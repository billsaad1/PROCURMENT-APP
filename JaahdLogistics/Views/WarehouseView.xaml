<UserControl x:Class="JaahdLogistics.Views.WarehouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JaahdLogistics.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource GRN}" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="{DynamicResource SelectPO}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding PendingPOs}" SelectedItem="{Binding SelectedPO}" DisplayMemberPath="PONumber" Width="200"/>
            <Button Content="{DynamicResource CreateGRN}" Command="{Binding CreateGRNCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding GrnItems}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource Description}" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="{DynamicResource OrderedQty}" Binding="{Binding OrderedQuantity}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="{DynamicResource ReceivedQty}" Binding="{Binding ReceivedQuantity}" Width="100"/>
                <DataGridTextColumn Header="{DynamicResource AcceptedQty}" Binding="{Binding AcceptedQuantity}" Width="100"/>
                <DataGridTextColumn Header="{DynamicResource RejectedQty}" Binding="{Binding RejectedQuantity}" Width="100"/>
                <DataGridTextColumn Header="{DynamicResource RejectReason}" Binding="{Binding RejectReason}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="{DynamicResource SaveGRN}" Command="{Binding SaveGRNCommand}" HorizontalAlignment="Right" Width="120" Height="35" Margin="0,15,0,0"/>
    </Grid>
</UserControl>
