<UserControl x:Class="JaahdLogistics.Views.WarehouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JaahdLogistics.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side: List -->
        <Grid Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="{DynamicResource GRNList}" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="{DynamicResource New}" Command="{Binding NewGRNCommand}" Margin="10,0,0,0"/>
            </StackPanel>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Grns}" SelectedItem="{Binding SelectedGRN}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="GRN #" Binding="{Binding GRNNumber}" Width="*"/>
                    <DataGridTemplateColumn Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="X" Command="{Binding DataContext.DeleteGRNCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#C0392B"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Right Side: Details -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource GRN}" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

            <Grid Grid.Row="1" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{DynamicResource SelectPO}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding PendingPOs}" SelectedItem="{Binding SelectedPO}" DisplayMemberPath="PONumber"/>
                <Button Grid.Column="2" Content="{DynamicResource CreateGRN}" Command="{Binding CreateGRNCommand}" Margin="10,0,0,0"/>

                <TextBlock Grid.Column="3" Text="Invoice #" VerticalAlignment="Center" Margin="20,0,10,0"/>
                <TextBox Grid.Column="4" Text="{Binding CurrentGRN.InvoiceNumber}"/>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="5" Orientation="Horizontal" Margin="0,10,0,0">
                    <CheckBox Content="Qty Comply" IsChecked="{Binding CurrentGRN.IsQtyComply}" Margin="0,0,20,0"/>
                    <CheckBox Content="Qty Matches" IsChecked="{Binding CurrentGRN.IsQtyMatch}" Margin="0,0,20,0"/>
                    <CheckBox Content="Qty Intact" IsChecked="{Binding CurrentGRN.IsQtyIntact}"/>

                    <TextBlock Text="Receiver:" VerticalAlignment="Center" Margin="20,0,10,0"/>
                    <ComboBox ItemsSource="{Binding Employees}" SelectedValue="{Binding CurrentGRN.ReceiverEmployeeId}" SelectedValuePath="Id" DisplayMemberPath="DisplayName" Width="150"/>
                </StackPanel>
            </Grid>

            <DataGrid Grid.Row="2" ItemsSource="{Binding GrnItems}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Description}" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="{DynamicResource OrderedQty}" Binding="{Binding OrderedQuantity}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="{DynamicResource ReceivedQty}" Binding="{Binding ReceivedQuantity}" Width="100"/>
                    <DataGridTextColumn Header="{DynamicResource AcceptedQty}" Binding="{Binding AcceptedQuantity}" Width="100"/>
                    <DataGridTextColumn Header="{DynamicResource RejectedQty}" Binding="{Binding RejectedQuantity}" Width="100"/>
                    <DataGridTextColumn Header="{DynamicResource RejectReason}" Binding="{Binding RejectReason}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="{DynamicResource Print}" Command="{Binding PrintCommand}" Width="120" Height="35" Margin="0,0,10,0"/>
                <Button Content="{DynamicResource SaveGRN}" Command="{Binding SaveGRNCommand}" Width="120" Height="35"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
