<UserControl x:Class="JaahdLogistics.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:JaahdLogistics.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer Margin="15">
        <StackPanel>
            <TextBlock Text="{DynamicResource ProjectSpendingReport}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding SpendingReport}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,20" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource Total}" Binding="{Binding TotalSpent, StringFormat=N2}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="{DynamicResource InventoryStatusReport}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding InventoryReport}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,20" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Description}" Binding="{Binding ItemDescription}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource Project}" Binding="{Binding ProjectName}" Width="150"/>
                    <DataGridTextColumn Header="{DynamicResource Quantity}" Binding="{Binding CurrentQuantity}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="{DynamicResource VendorHistoryReport}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding VendorReport}" AutoGenerateColumns="False" IsReadOnly="True" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding VendorName}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource OrderCount}" Binding="{Binding OrderCount}" Width="100"/>
                    <DataGridTextColumn Header="{DynamicResource TotalValue}" Binding="{Binding TotalValue, StringFormat=N2}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
