<UserControl x:Class="JaahdLogistics.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:JaahdLogistics.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource ProjectSpendingReport}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding SpendingReport}" AutoGenerateColumns="True" IsReadOnly="True" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="2" Text="{DynamicResource InventoryStatusReport}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <DataGrid Grid.Row="3" ItemsSource="{Binding InventoryReport}" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</UserControl>
