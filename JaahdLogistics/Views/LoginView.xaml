<UserControl x:Class="JaahdLogistics.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Center" VerticalAlignment="Center">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="20" CornerRadius="5" Background="White">
        <StackPanel Width="250">
            <TextBlock Text="{DynamicResource Login}" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <Label Content="{DynamicResource Username}"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="{DynamicResource Password}"/>
            <PasswordBox x:Name="PassBox" Margin="5"/>
            <Button Content="{DynamicResource Login}"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PassBox}"
                    Margin="0,20,0,0"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
