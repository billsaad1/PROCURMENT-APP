<UserControl x:Class="JaahdLogistics.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Cards -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- 1. Header Area -->
        <Grid Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Logistics Dashboard" FontSize="26" FontWeight="Bold" Foreground="#1B4F72"/>
                <Button Command="{Binding RefreshDashboardCommand}" Margin="15,0,0,0" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <TextBlock Text="🔄" FontSize="18" VerticalAlignment="Center"/>
                </Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Border Background="#EBF5FB" CornerRadius="15" Padding="15,5" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                         <Ellipse Width="8" Height="8" Fill="{Binding IsSyncing, Converter={StaticResource BoolToColorConverter}}" Margin="0,0,8,0"/>
                         <TextBlock Text="{Binding SyncStatus}" FontSize="12" VerticalAlignment="Center" Foreground="#2E86C1"/>
                    </StackPanel>
                </Border>
                <Button Content="Force Sync" Command="{Binding SyncNowCommand}"
                        IsEnabled="{Binding IsSyncing, Converter={StaticResource InverseBooleanConverter}}"
                        Background="#1B4F72" Foreground="White" BorderThickness="0" Padding="15,7"/>
            </StackPanel>
        </Grid>

        <!-- 2. Summary Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,20">
            <!-- Projects Card -->
            <Border Margin="5" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect><DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="Total Projects" Foreground="Gray" FontSize="12"/>
                    <TextBlock Text="{Binding TotalProjects}" FontSize="24" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Text="Active Contracts" FontSize="10" Foreground="#27AE60"/>
                </StackPanel>
            </Border>

            <!-- PR Card -->
            <Border Margin="5" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect><DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="Pending PRs" Foreground="Gray" FontSize="12"/>
                    <TextBlock Text="{Binding PendingPRs}" FontSize="24" FontWeight="Bold" Margin="0,5" Foreground="#D35400"/>
                    <TextBlock Text="Awaiting Approval" FontSize="10" Foreground="#E67E22"/>
                </StackPanel>
            </Border>

            <!-- PO Card -->
            <Border Margin="5" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect><DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="Approved POs" Foreground="Gray" FontSize="12"/>
                    <TextBlock Text="{Binding ApprovedPOs}" FontSize="24" FontWeight="Bold" Margin="0,5" Foreground="#27AE60"/>
                    <TextBlock Text="Ready for Delivery" FontSize="10" Foreground="#2ECC71"/>
                </StackPanel>
            </Border>

            <!-- Spending Card -->
            <Border Margin="5" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect><DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/></Border.Effect>
                <StackPanel>
                    <TextBlock Text="Total Spending" Foreground="Gray" FontSize="12"/>
                    <TextBlock Text="{Binding TotalSpending, StringFormat=N0}" FontSize="24" FontWeight="Bold" Margin="0,5"/>
                    <TextBlock Text="Across all Projects" FontSize="10" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- 3. Lists and Visuals -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Main Tables -->
            <TabControl Background="Transparent" BorderThickness="0" Margin="0,0,10,0">
                <TabItem Header="My Requisitions">
                    <Border Background="White" CornerRadius="8" Padding="10">
                        <DataGrid ItemsSource="{Binding UserPRs}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="PR No" Binding="{Binding PRNumber}" Width="120"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="100"/>
                                <DataGridTextColumn Header="Project" Binding="{Binding Project.Code}" Width="80"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="5,2"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="FinalApproved"><Setter Property="Foreground" Value="Green"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Pending"><Setter Property="Foreground" Value="Orange"/></DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Rejected"><Setter Property="Foreground" Value="Red"/></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>

                <TabItem Header="Project Budgets">
                    <Border Background="White" CornerRadius="8" Padding="15">
                        <ItemsControl ItemsSource="{Binding ProjectSpending}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,15">
                                        <Grid Margin="0,0,0,5">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                            <TextBlock HorizontalAlignment="Right" Foreground="Gray" FontSize="11">
                                                <Run Text="{Binding Spent, StringFormat=N0}"/> / <Run Text="{Binding Budget, StringFormat=N0}"/>
                                            </TextBlock>
                                        </Grid>
                                        <Grid Height="12">
                                            <Border Background="#F2F3F4" CornerRadius="6"/>
                                            <Border HorizontalAlignment="Left" CornerRadius="6" Width="{Binding PercentSpent, StringFormat={}{0}%}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#2ECC71"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ProgressColor}" Value="Orange"><Setter Property="Background" Value="#F1C40F"/></DataTrigger>
                                                            <DataTrigger Binding="{Binding ProgressColor}" Value="Red"><Setter Property="Background" Value="#E74C3C"/></DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </Grid>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </TabItem>
            </TabControl>

            <!-- Right: Action Center -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Action Required" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1B4F72"/>
                <Border Background="White" CornerRadius="8" Padding="10">
                    <Border.Effect><DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/></Border.Effect>
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding ActionItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#F2F3F4" BorderThickness="0,0,0,1" Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding PRNumber}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding Status}" FontSize="10" Foreground="Orange"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="Review →" VerticalAlignment="Center" Foreground="#3498DB" Cursor="Hand" FontSize="11"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="No pending approvals" HorizontalAlignment="Center" Margin="20" Foreground="LightGray">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers><DataTrigger Binding="{Binding ActionItems.Count}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
