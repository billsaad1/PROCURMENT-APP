<UserControl x:Class="JaahdLogistics.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:JaahdLogistics.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <localhelpers:BindingProxy x:Key="ViewProxy" xmlns:localhelpers="clr-namespace:JaahdLogistics.Helpers"/>
    </UserControl.Resources>
    <ScrollViewer Margin="20">
        <StackPanel>
            <TextBlock Text="{DynamicResource Settings}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <GroupBox Header="{DynamicResource AssociationInfo}" Margin="0,0,0,20" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="{DynamicResource AssociationName}" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Grid.Column="1" Text="{Binding Settings.AssociationName}" Margin="5"/>

                    <TextBlock Grid.Row="1" Text="Address" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Settings.Address}" Margin="5"/>

                    <TextBlock Grid.Row="2" Text="Contact Info" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Settings.ContactInfo}" Margin="5"/>

                    <TextBlock Grid.Row="3" Text="{DynamicResource Logo}" VerticalAlignment="Center" Margin="5"/>
                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                        <Border Width="100" Height="100" BorderBrush="Gray" BorderThickness="1" Margin="5">
                             <Image Source="{Binding Settings.LogoImage, Converter={StaticResource ImageBytesConverter}}" Stretch="Uniform"/>
                        </Border>
                        <Button Content="{DynamicResource UploadLogo}" Command="{Binding UploadLogoCommand}" VerticalAlignment="Bottom" Margin="5"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Header="{DynamicResource UserSignature}" Margin="0,0,0,20" Padding="10">
                <StackPanel Orientation="Horizontal">
                    <Border Width="150" Height="60" BorderBrush="Gray" BorderThickness="1" Margin="5">
                        <Image Source="{Binding CurrentUserSignature, Converter={StaticResource ImageBytesConverter}}" Stretch="Uniform"/>
                    </Border>
                    <Button Content="{DynamicResource UploadSignature}" Command="{Binding UploadSignatureCommand}" VerticalAlignment="Bottom" Margin="5"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="User Management" Margin="0,0,0,20" Padding="10">
                <StackPanel>
                    <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False" Height="200" Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="100"/>
                            <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTemplateColumn Header="Role" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding Source={StaticResource ViewProxy}, Path=Data.Roles}"
                                                  SelectedItem="{Binding Role, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="X" Command="{Binding Data.RemoveUserCommand, Source={StaticResource ViewProxy}}" CommandParameter="{Binding}" Background="#C0392B"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Content="Add User" Command="{Binding AddUserCommand}" HorizontalAlignment="Left" Width="100"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="{DynamicResource TermsConditions}" Padding="10">
                <StackPanel>
                    <TextBlock Text="{DynamicResource PRTerms}" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Settings.PRTerms}" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,0,0,10"/>

                    <TextBlock Text="{DynamicResource RFQTerms}" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Settings.RFQTerms}" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,0,0,10"/>

                    <TextBlock Text="{DynamicResource POTerms}" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding Settings.POTerms}" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,0,0,10"/>
                </StackPanel>
            </GroupBox>

            <Button Content="{DynamicResource Save}" Command="{Binding SaveCommand}" Width="120" Height="35" HorizontalAlignment="Right" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
