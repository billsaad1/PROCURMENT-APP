<Window x:Class="JaahdLogistics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:JaahdLogistics.ViewModels"
        xmlns:views="clr-namespace:JaahdLogistics.Views"
        mc:Ignorable="d"
        Title="Jaahd Logistics Management System" Height="600" Width="1000">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProjectViewModel}">
            <Grid>
                <TextBlock Text="Project Management Dashboard placeholder" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F0F0F0" Padding="5">
            <StackPanel Orientation="Horizontal">
                <Button Content="English" Command="{Binding SwitchLanguageCommand}" CommandParameter="en" Width="80"/>
                <Button Content="العربية" Command="{Binding SwitchLanguageCommand}" CommandParameter="ar" Width="80"/>
                <TextBlock Text="{Binding CurrentUser.FullName}" Margin="20,0" VerticalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
